import React from 'react';

const CompStickyInner = ({ children }) => {
    return (
        <div className='sticky_inner'>
            {children}
        </div>
    );
};

export default CompStickyInner;